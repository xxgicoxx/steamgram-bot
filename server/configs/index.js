const postgresqlConfig = require('./postgresql');
const steamConfig = require('./steam');
const telegramConfig = require('./telegram');

module.exports = {
  postgresqlConfig,
  steamConfig,
  telegramConfig,
};
